# Public browser configuration. This is the Render FastAPI origin, not a secret.
NEXT_PUBLIC_CIVICLENS_API_BASE_URL=http://localhost:8000

# Public canonical site origin used to build absolute social metadata URLs.
NEXT_PUBLIC_CIVICLENS_SITE_URL=http://localhost:3000
